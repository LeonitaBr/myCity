import { environment } from "./../environments/environment";

export const PLACES = environment + 'api/' + 'posts/';
export const AUTH = environment + 'api/' + 'user';
export const RESTAURANTS = environment + 'api/' + 'restaurants';