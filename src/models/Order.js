class Order {
    constructor( items, totalAmount, date) {
            this.items = items,
            this.totalAmount = totalAmount,
            this.date = date
    }
    

}

export default Order;